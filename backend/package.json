{
  "name": "servifibras-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend aislado para generar descripciones de producto via API de Claude (foto -> descripcion 6 bloques + correccion de categoria).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
